#!/bin/bash

sudo modprobe -r psmouse
#sudo modprobe psmouse proto=imps
# sudo modprobe psmouse resync_time=10 //10 seems legit to me atleast..

# sudo modprobe psmouse proto=bare
# modprobe psmouse resync_time=10 //10 seems legit to me atleast..

sudo modprobe psmouse synaptics_intertouch=1 proto=exps
